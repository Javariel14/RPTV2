# Local reference only. Never put real customer data into this environment.
RPT_ENV=local
RPT_RELEASE_APPROVED=false
# Supabase issuer, after tenant-auth topology and operator approval.
AUTH_ISSUER=https://unconfigured.invalid/auth/v1
# Runtime secrets belong in Hyperdrive or an approved secret store, not NEXT_PUBLIC_*.
# Local tests generate separate random runtime/migrator passwords in memory.
